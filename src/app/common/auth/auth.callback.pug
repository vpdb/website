include ../../template/_mixins

div.text-center.padder-top-2x.padder-bottom-2x(ng-controller="AuthCallbackCtrl as vm")
	div(ng-hide="vm.error")
		+icon('circle-o-notch').svg-spin.svg-4x
		h3 Authenticating...

	div(ng-show="vm.error")
		h3 Oops!
		p(ng-show="vm.error.error && vm.error.error_uri") Something went wrong <a ng-href="{{ vm.error.error_uri }}" target="_blank">({{ vm.error.error }})</a>.
		p(ng-hide="vm.error.error && vm.error.error_uri") Something went wrong.
		p(ng-show="vm.error.error_description") {{ vm.error.error_description }}
		p(ng-show="vm.error.error === 'access_denied'") Looks like you didn't allow access from VPDB.

		br
		a(ui-sref="home")
			+icon('angle-left').smaller.shift-up.space-right
			| Back Home
